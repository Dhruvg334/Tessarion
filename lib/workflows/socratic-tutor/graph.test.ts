import { describe, expect, it } from 'vitest';
import { runSocraticTutor } from './graph';
const base={runId:'11111111-1111-4111-8111-111111111111',traceId:'22222222-2222-4222-8222-222222222222',workspaceId:'33333333-3333-4333-8333-333333333333',userId:'44444444-4444-4444-8444-444444444444',sessionId:'55555555-5555-4555-8555-555555555555',conceptId:'66666666-6666-4666-8666-666666666666',focusType:'misconception' as const,focusSummary:'Confuses contiguous storage with linked nodes.',sourceChunkIds:['chunk-1'],previousTurns:[],learnerResponse:'I think both use direct indexes.',maxTurns:8};
describe('socratic tutor workflow',()=>{ it('returns one bounded question',async()=>{const r=await runSocraticTutor(base); expect(r.status).toBe('waiting_for_learner'); expect((r.tutorMessage?.match(/\?/g)??[]).length).toBeLessThanOrEqual(1);}); it('interrupts while waiting for learner input',async()=>{const r=await runSocraticTutor({...base,previousTurns:[{id:'turn-1',tutoringSessionId:base.sessionId,workspaceId:base.workspaceId,userId:base.userId,role:'tutor',turnType:'socratic_question',content:'What differs?',sourceChunkIds:[],gapFindingIds:[],masterySignalIds:[],tutorMove:'ask_contrast_question',createdAt:'2026-01-01T00:00:00.000Z'}],learnerResponse:null}); expect(r.status).toBe('waiting_for_learner'); expect(r.stepTrace.at(-1)?.outcome).toBe('interrupt');});});
